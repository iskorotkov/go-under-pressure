{
  "name": "urlshortener-bench",
  "private": true,
  "type": "module",
  "scripts": {
    "seed": "bun run seed.ts",
    "create": "[ -f .env ] && source .env; k6 run create.ts",
    "redirect": "[ -f .env ] && source .env; k6 run redirect.ts",
    "mixed": "[ -f .env ] && source .env; k6 run mixed.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/k6": "^1.4.0",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
